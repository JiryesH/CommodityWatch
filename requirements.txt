# Core scraper dependencies
feedparser
requests
curl_cffi
beautifulsoup4

# Optional — ML enrichment (only needed locally, not in GitHub Actions)
# Uncomment to install:
# transformers
# torch
# spacy

# PriceWatch API server
sqlalchemy
PyYAML
