# Optional if the backend repo is not discoverable as a sibling "Commodity Prices" folder.
COMMODITY_BACKEND_ROOT=/absolute/path/to/Commodity Prices

# Matches the backend pipeline setting. Relative sqlite paths resolve from COMMODITY_BACKEND_ROOT.
DATABASE_URL=sqlite:///data/commodities.db

# Frontend/API server port.
PORT=8080
